
## 2026-06-09 — Visual Field Appendix (frontend asset only)
- New public document: `frontend/public/docs/Ghost_Operator_Training_Visual_Appendix.pdf` (27 pages, real console screenshots with annotated instruction layer; source: `tools/operator-training/appendix-build.mjs` + `tools/operator-training/shots/` + `operator-training-appendix.html` at repo root).
- Not yet linked from any page — candidates: second download in the training waitlist gate. No backend changes.

## 2026-06-09 — Appendix download in app Settings (frontend-only)
- `SettingsPanel.tsx` (Learning section) now offers a direct download of `/docs/Ghost_Operator_Training_Visual_Appendix.pdf`; new i18n keys `settingsAppendix*`. No backend changes.

## 2026-06-10 07:39 — Full deploy (backend+frontend)
- **Trigger:** "גו דיפלוי" (user ran the rashi-deploy skill)
- **Target:** ghst-rashi (frontend+backend)
- **Result:** ✅ success
- **Wall-clock:** total ~13m42s (Cloud Run build+deploy ~12m — warm but heavy image rebuild; frontend build 2.8s; hosting ~30s)
- **Drift since last deploy:** large — 89 uncommitted changes. Backend: migrations 021 (conversation_title_source) + 022 (user_origin_lead), routes (conversations, users), services (incident, openai_client), stores (conversation, user), schemas. Frontend: capabilities page rebuild (`components/capabilities/*`), site copy modules (`src/site/copy/*`), operator training pages + PDFs in `public/docs/`, TrialSetupWizard, DemoAdminUserPicker, GhostHighFive, autoNaming/celebration/siteLocale stores, i18n updates. Backend fingerprint `4a7c8786…` → `1ed6728a…` (marker updated).
- **Failures hit & fixes:** pre-deploy blockers only — (1) gcloud token expired ("Reauthentication failed", cannot auto-refresh) → user ran `~/google-cloud-sdk/bin/gcloud auth login` interactively (note: gcloud was NOT on PATH in the user's own zsh either — full path needed). (2) `OPENAI_API_KEY` not exported → fetched both `OPENAI_API_KEY` and `GHOST_MASTER_KEY` from Secret Manager via `gcloud secrets versions access latest` and exported before running the script — this preserved the existing GHOST_MASTER_KEY (44ch) instead of letting the script regenerate it. Secret versions bumped to v9 (same values re-upserted). Deploy itself: zero failures.
- **Live verify:** root 200, app.html 200, `/api/health` ok (via Hosting rewrite AND direct Cloud Run). New revision `ghst-api-00008-l84`. Note: gcloud now prints service URL as `https://ghst-api-856742618724.us-central1.run.app` — the old `ghst-api-gsapdvgxda-uc.a.run.app` URL still works (both alias the same service).
- **Next-deploy notes:** fetching secrets from Secret Manager before the script is the proven way to avoid the `OPENAI_API_KEY not set` blocker — do this every time. Cloud Run "warm" rebuild took ~12m (longer than the 3–6m estimate), likely due to requirements layer invalidation; budget ~12–15m for full deploys after backend changes.

## 2026-06-11 — Dev server moved to plain HTTP (no deploy impact)
- Removed `@vitejs/plugin-basic-ssl` from `frontend/vite.config.ts` + uninstalled the package (`package.json`/`package-lock.json` changed). Dev now serves `http://localhost:8888` (localhost is a secure context over http — camera/mic unaffected). Production is untouched (Firebase Hosting serves HTTPS; basicSsl only ever affected the dev server).
- Updated skills `8go-restart`, `open-in-cursor-browser`, `ghost-full-test` to http + removed all CDP cert-bypass steps; deleted the stale `dev-server-guardian` skill (pointed at the old `ghost-local-ai` project and opened Safari).
- If any doc/skill still says `https://localhost:8888` — it's stale; fix to http.

## 2026-06-14 13:21 — Full deploy (backend+frontend)
- **Trigger:** user asked "דיפלוי פרונט" → "גו פרונט" path correctly ABORTED (backend changed since last deploy) → user re-authed → ran full "גו דיפלוי".
- **Target:** ghst-rashi (frontend+backend).
- **Result:** ✅ success.
- **Wall-clock:** total ~12m27s (script elapsed 746s) — Cloud Run build+deploy ~12m (heavy torch/Chroma image), frontend build 3.83s, hosting ~30s.
- **Drift since last full deploy (2026-06-10, marker `1ed6728a…`):** scheduled-tasks backend feature (`migration 023_scheduled_tasks.sql`, `routes/tasks.py`, `services/task_service.py`, `storage/task_store.py`, `main.py` wiring) + earlier uncommitted backend/frontend work. Backend fingerprint `1ed6728a…` → `70aa8725fd305c89eb5f83534d38d10d2cfb68f9c1830f16afbcadedb85ae881` (marker updated).
- **Failures hit & fixes:** pre-deploy only — both Firebase AND gcloud tokens expired ("credentials are no longer valid" / "Reauthentication failed"). User ran `firebase login --reauth` + `~/google-cloud-sdk/bin/gcloud auth login` interactively (note: user's first attempt failed because both commands were pasted on one line with inline `#` comments → zsh glob error; fixed by running each on its own line, no comments). Secrets fetched from Secret Manager and exported before the script (preserved GHOST_MASTER_KEY 44ch, OPENAI 164ch); both secrets re-upserted as version 10. Deploy itself: zero failures.
- **Live verify:** root 200, app.html 200, `/api/health` → `{"ok":true,"data":{"status":"healthy"}}`. New Cloud Run revision `ghst-api-00009-mm6`, serving 100% traffic. Service URL `https://ghst-api-856742618724.us-central1.run.app`.
- **Next-deploy notes:** backend + frontend now in sync in cloud → a "גו פרונט" will pass the fingerprint gate again (until backend changes next). Reminder: pasting multi-line auth commands with inline comments breaks in zsh — give the user each command on its own line with no `#` comment.

## 2026-06-14 12:55 — Skill review & sync (no deploy)
- **Trigger:** user asked to review the project and bring `rashi-deploy` current.
- **Target:** none deployed — documentation/skill sync only.
- **Drift since last deploy (2026-06-10, marker `1ed6728a…`):** still uncommitted (HEAD = `8a0c398`, 2026-06-08). New backend feature **scheduled tasks**: migration `023_scheduled_tasks.sql`, `routes/tasks.py`, `services/task_service.py`, `storage/task_store.py` (+ `main.py` route wiring) — backend changed vs. the deployed image, so the next "גו פרונט" will (correctly) ABORT and require a full "גו דיפלוי". Frontend also grew tasks UI (`components/tasks/*`, `services/taskEngine.ts`, `stores/taskStore.ts`).
- **Skill updates applied to SKILL.md:**
  1. Documented the **product-knowledge bridge**: `frontend/src/data/capabilities.ts` → `npm run gen:knowledge` (`scripts/gen-ghost-knowledge.mjs`) → `backend/app/data/ghost_capabilities.json`, baked into the backend image via `.gcloudignore` re-include. `npm run build` is now `gen:knowledge && tsc -b && vite build`, and `deploy-firebase.sh` step 6c regenerates it before the backend build.
  2. **Gotcha (Hard-Won Lesson #8):** `backend-fingerprint.sh` prunes every `data/` dir, so a `capabilities.ts` edit does NOT change the backend fingerprint → "גו פרונט" would ship stale Ghost knowledge to the cloud. Rule: capabilities change ⇒ full "גו דיפלוי".
  3. **Hard-Won Lesson #7:** `CLOUDSDK_PYTHON` — gcloud needs Python 3.10–3.14; system 3.9 makes it falsely report "not authenticated". The deploy script now auto-selects a compatible interpreter.
  4. Cloud Run URL: recorded the new form `https://ghst-api-856742618724.us-central1.run.app` (old `…gsapdvgxda-uc…` alias still works).
  5. Self-update trigger list + Troubleshooting table extended accordingly.
- **Next-deploy notes:** backend changed (scheduled tasks) → next deploy must be a FULL "גו דיפלוי"; "גו פרונט" will abort on the fingerprint gate until then.

## 2026-06-17 — Production hardening (security + stability, no deploy yet)
- **Trigger:** full production-readiness review → phased hardening implementation.
- **New REQUIRED secrets for the next deploy** (the deploy script now upserts them and FAILS without `GHOST_DEMO_API_KEY`):
  - `GHOST_DEMO_API_KEY` — shared demo/trial OpenAI key, **server-side only** (was previously hardcoded in `frontend/src/config/demoAccess.ts` and shipped in the bundle — that key MUST be rotated/revoked in the OpenAI dashboard).
  - `GHOST_ADMIN_TOKEN` — guards internal PII endpoints; auto-generated by the deploy script if unset (value withheld from logs, stored in Secret Manager). Operators present it via the `X-Ghost-Admin-Token` header / `?admin_token=` (the demo-admin 8+0 and downloads chords now prompt for it).
  - `GHOST_GM_CODE` — operator-provisioning code, server-verified (optional; empty disables provisioning).
- **Startup is now fail-fast** on a missing/placeholder/invalid `GHOST_MASTER_KEY` (`assert_master_key_safe()` in `config.py`, called in `main.py` lifespan). Ensure the Secret Manager key is a valid 44-char Fernet key.
- **Single-instance limitation (unchanged, now explicit):** `AlertBroker`/`AlertQueue`, the rate limiter (`services/rate_limiter.py`), and the OpenAI client cache are in-process; SQLite/Chroma are local files. Deploy MUST stay `--max-instances 1` (single worker). Scaling out requires Redis-backed pub/sub + rate limits and a managed DB (deferred).
- **Other hardening in this change:** IDOR fixes (ownership on conversation PATCH/DELETE/auto-title + memory delete), admin-gated `/users`, `/downloads`, `/applications`, magic-link mint; per-IP rate limits on chat/broadcast/detection/knowledge + public forms; base64 payload caps; knowledge upload size/type allowlist; `/uploads/applications/` (CV PII) blocked behind admin; CORS methods/headers tightened; graceful `AlertQueue` shutdown; removed debug telemetry; CI added (`.github/workflows/ci.yml`).
- **Next-deploy notes:** backend changed substantially → next deploy MUST be a FULL "גו דיפלוי"; export `GHOST_DEMO_API_KEY` (and optionally `GHOST_GM_CODE`) before running, and capture the generated `GHOST_ADMIN_TOKEN` from Secret Manager for operator use.

## 2026-06-17 — Owner / Super-Admin panel (`/admin`) — Stages 1-10 (no deploy yet)
- **Trigger:** "התחל" → full implementation of the `ghost-owner-admin-panel` plan after Production Hardening completed.
- **What it is:** a SEPARATE, secure Owner console at `/admin` (new `admin.html` MPA bundle), decoupled from the operator trust-the-client model. Auth = email + password (argon2id) + TOTP 2FA + short-lived JWT access + rotating refresh tokens; RBAC (owner/admin/support/finance/viewer). Screens: Overview, Users, Audit, Usage, Costs, Errors, System Health. All Hebrew RTL, Ghost design tokens.
- **New migration:** `027_admin_panel.sql` — tables `admin_users`, `admin_refresh_tokens`, `audit_log`, `usage_events`, `llm_usage`, `error_events`; + `users` lifecycle columns (`status`, `deleted_at`, `last_login_at`, `admin_note`). Idempotent ALTERs; verified on a copy of the live 39-user DB (all default to `status='active'`).
- **New backend pip deps (MUST be in the Cloud Run image):** `argon2-cffi==23.1.0`, `PyJWT==2.10.1`, `pyotp==2.9.0` (added to `requirements.txt`).
- **New env (optional, recommended):** `GHOST_ADMIN_JWT_SECRET` — signs admin JWTs; if unset, derived from `GHOST_MASTER_KEY` (works out of the box). Optional `ADMIN_ACCESS_TTL_MINUTES` (30), `ADMIN_REFRESH_TTL_DAYS` (7). No new REQUIRED secret.
- **Owner bootstrap (run ONCE in each environment — no public sign-up):** `cd backend && python scripts/create_admin.py --email you@example.com --role owner` (interactive password prompt; 2FA enrolled on first login). In cloud, run against the deployed DB with the same `GHOST_MASTER_KEY`.
- **Frontend build:** `admin.html` added to `vite.config.ts` `rollupOptions.input` → emits `dist/admin.html` + a code-split `admin-*.js` (~45 kB / ~11.5 kB gzip, fully isolated from site/app bundles). Dev SPA-fallback maps `/admin`→`/admin.html`.
- **Firebase routing:** `firebase.json` adds rewrite `/admin`→`/admin.html` (before the `**` catch-all) + a no-cache header on `/admin.html`. `/api/admin/**` is served by the same Cloud Run service (covered by the existing `/api/**` rewrite).
- **Cost capture:** `openai_client.py` now records token usage → `llm_usage` (best-effort) at chat (streaming, via `stream_options.include_usage`), alert vision, structured vision, and embeddings. Pricing in `config.py` (`estimate_cost_usd`, longest-prefix match — fixed a `gpt-4o`/`gpt-4o-mini` shadow bug). No per-call behavior change beyond the extra usage chunk.
- **Global error handler:** `@app.exception_handler(Exception)` in `main.py` records 500-class failures to `error_events` and returns the standard envelope; `GhostException`/`HTTPException` paths are unchanged.
- **Verification:** new standalone smoke `backend/tests/test_admin_smoke.py` (29/29 pass); full smoke suite green (migrations/incidents/detection/rate-limiter/product-knowledge); `ruff` clean across `app/`; `vite build` green; admin code type-clean under `tsconfig.app.json`; live-verified on the running stack (login→2FA→users list of 39 real operators), temp verification admin removed afterwards (real DB has 0 admins).
- **Known unrelated issue:** `tsc -b` currently fails on `frontend/src/stores/messageStore.test.ts` (TS2556) — that file was modified at 19:37 by a concurrent process, NOT part of this work; left untouched.
- **Next-deploy notes:** FULL "גו דיפלוי" required (backend deps + migration + new bundle). After deploy, run the owner-bootstrap CLI once in the cloud env, then sign in at `https://<host>/admin`. Single-instance constraints unchanged (audit/usage/cost/error writes are tiny local SQLite inserts; no new cross-instance state).

## 2026-06-17 20:44 — Full deploy (backend+frontend) — demo/admin secret-mapping fix + key rotation
- **Trigger:** user approved "do everything" after the trial-bug fix chat (rotate demo key + push cloud to parity).
- **Target:** ghst-rashi (frontend+backend), PRODUCTION.
- **Result:** ✅ success (core deploy). One non-fatal post-step error (see below).
- **Wall-clock:** ~14m total (Cloud Run build ~13m heavy torch/Chroma; hosting upload+release ~40s; verify ~10s).
- **Root cause fixed in code:** `backend/app/config.py` fields `demo_api_key`/`admin_token`/`admin_jwt_secret` had bare names so pydantic looked for `DEMO_API_KEY`/`ADMIN_TOKEN`, but `.env` AND deploy inject `GHOST_DEMO_API_KEY`/`GHOST_ADMIN_TOKEN` → they loaded EMPTY → `/api/users/demo/trial` returned 503 DEMO_UNAVAILABLE and admin endpoints were CLOSED, in BOTH local and cloud. Added `validation_alias=AliasChoices(...)` so the GHOST_-prefixed names load. Also added a boot warning in `main.py` when these are empty.
- **Secrets:** `GHOST_DEMO_API_KEY` and `GHOST_ADMIN_TOKEN` were created as **version 1** in Secret Manager (never existed before → confirms the cloud demo/admin server flow had never worked). OPENAI/MASTER re-upserted as v15. Demo key value is the ROTATED key from local `backend/.env` (old exposed key NOT yet revoked by operator; git-history scrub blocked on missing GitHub auth — see below).
- **Also shipped (came along in the local working tree):** the admin-panel backend from the 2026-06-17 entry above (migration `027_admin_panel.sql`, deps `argon2-cffi`/`PyJWT`/`pyotp`, `admin.html`). Cloud booted clean with them (health ok), so migration 027 ran on the cloud DB. Owner-bootstrap CLI still needs to be run once in cloud (separate task).
- **Failures hit & fixes:** final `scripts/sync-demo-admin.sh` step FAILED — "could not parse DEMO_API_KEY from demoAccess.ts". This script is now OBSOLETE: the security hardening removed the key from `frontend/src/config/demoAccess.ts` (it lives server-side as `GHOST_DEMO_API_KEY`). The new trial flow (`/users/demo/trial`) uses `settings.demo_api_key` directly, so no cloud-DB key sync is needed. Verified the real flow works in cloud (trial → 201), so the sync error is cosmetic. TODO: update `deploy-firebase.sh`/`deploy-frontend.sh` to drop or rewrite the `sync-demo-admin.sh` call so a full deploy doesn't end on a red ERROR.
- **Live verify:** root 200, app.html 200, `/api/health` ok, **`/api/users/demo/trial` → 201** (rotated key + alias fix work in prod), admin `/api/users/trial-accounts` 200 with token / 403 without.
- **Auth state:** gcloud + firebase both valid as omer@ghost-il.com (no re-auth needed this run).
- **Still open (operator-only):** (1) revoke the OLD exposed demo key `sk-proj-JxDt7dM5…` in the OpenAI dashboard; (2) scrub it from git history (commit `c6ccff8` on branch `snapshot-2026-06-17-1740`, file `demoAccess.ts`) + force-push — BLOCKED: no working GitHub auth in this env (`gh` binary missing at `~/bin/gh`, credential helper broken, `git ls-remote origin` fails).

## 2026-06-18 — Bounded self-knowledge + Ghost Expert mode (no deploy yet)
- **Trigger:** "Ghost — ידע מקצועי תחום + מצב Expert" plan implementation.
- **What it is (two features):**
  1. **Bounded professional self-knowledge.** When an operator asks "what can you do / how do I use you / how does X work in Ghost", Ghost now answers ONLY from a fixed set of official source docs (operator training program + visual appendix HE, shared-language partners, enterprise architecture) and offers the matching PDF for download in chat. The 9-capability block stays as the identity anchor; the docs are a deeper bounded layer.
  2. **Ghost Expert mode.** Operator types `expert` in the composer → smoked-glass overlay → multi-turn intelligence interrogation (`mode="expert"`, flagship model) → on readiness Ghost asks to pull a live frame → operator consents verbally → 4.2–6.8s thinking overlay → a light-theme PDF with 8 tasks + 8 alerts tailored to the environment + an in-chat "set up as inactive drafts" button.
- **New migration:** `028_expert_reports.sql` (table `expert_reports`). Runs automatically on boot via `run_migrations()`.
- **New global ChromaDB collection `ghost_self_knowledge`** (single-tenant, not per-user). **REQUIRED post-deploy step on Cloud Run:** seed it once so the bounded answers + doc offers work —
  `cd backend && python scripts/seed_self_knowledge.py` (reads the 4 source PDFs from the repo root; embeddings use `GHOST_DEMO_API_KEY`/`OPENAI_API_KEY`). Until seeded, the chat path degrades gracefully to the always-injected 9-capability block (no errors, just no deep doc retrieval / download offers). The source PDFs are baked into the image only if present under the build context; ensure `Ghost_Operator_Training_Program_HE.pdf`, `Ghost_Operator_Training_Visual_Appendix_HE.pdf`, `Ghost_Shared_Language_Partners.pdf`, `Ghost_Enterprise_Architecture_wecL.pdf` are reachable when running the seed (repo root by default; override `--docs-dir`).
- **New optional env:** `GHOST_EXPERT_MODEL` (defaults to `GHOST_VISION_MODEL` / flagship). No new REQUIRED secret.
- **Served assets:** copied `Ghost_Operator_Training_Program_HE.pdf` + `Ghost_Operator_Training_Visual_Appendix_HE.pdf` into `frontend/public/docs/` (the other two were already there) so the in-chat download card resolves over Firebase Hosting.
- **Backend touch:** `vector_store.py` (self-knowledge collection), `product_knowledge.py` (broadened detector + bounded block + doc map + strengthened guardrail), `knowledge_service.py` (`retrieve_self_knowledge`), `prompt_builder.py` (Expert prompts + `self_knowledge_chunks` injection), `chat_service.py` (self-knowledge retrieval + doc-offer marker + `_handle_expert_message`), `openai_client.py` (`generate_expert_recommendations`), `routes/expert.py` (generate/get/apply), `schemas/requests.py`, `config.py`, `storage/expert_store.py`, `main.py` (router). Frontend: `expertStore`, `ExpertOverlay`, `DocOfferCard`/`ExpertReportCard`, `docOfferMarker`/`expertReportMarker`, light-theme `ghostPdfShell` + `expertReportPdf`, Composer trigger/routing, MessageBubble wiring, i18n he/en.
- **Verification:** backend `py_compile` clean across all touched modules; frontend `tsc -b` clean; ESLint clean on new/edited files.
- **Next-deploy notes:** backend changed (migration 028 + new routes/deps-free) → next deploy MUST be a FULL "גו דיפלוי"; after deploy run `seed_self_knowledge.py` once in the cloud env (same `GHOST_MASTER_KEY`/chroma path) so self-knowledge retrieval is populated. "גו פרונט" will (correctly) abort on the fingerprint gate until a full deploy lands.

## 2026-06-18 03:17 — Full deploy (backend+frontend) — self-knowledge + Expert mode
- **Trigger:** "godeploy" (user ran the rashi-deploy skill).
- **Target:** ghst-rashi (frontend+backend), PRODUCTION.
- **Result:** ✅ success (after a hosting retry — see below).
- **Wall-clock:** ~19m total. Cloud Run build+deploy ~14m (heavy torch/Chroma image rebuild); frontend build 4.75s; hosting failed twice on a transient upload error then succeeded in ~45s after a Node flag fix.
- **Drift since last full deploy (2026-06-17 20:44, marker `86525a0a…` is the NEW one):** the "Bounded self-knowledge + Ghost Expert mode" feature — migration `028_expert_reports.sql`, `routes/expert.py`, new `ghost_self_knowledge` Chroma collection, plus backend touches in `vector_store.py`/`product_knowledge.py`/`knowledge_service.py`/`prompt_builder.py`/`chat_service.py`/`openai_client.py`/`storage/expert_store.py`/`config.py`/`main.py`; frontend Expert overlay + doc-offer/expert-report cards + light-theme PDF shell + Composer/MessageBubble wiring + i18n. Backend fingerprint → `86525a0a698ff3c967f40707cef90b912cffda87e3cb7f785a3b0a08115066e4` (marker updated).
- **Secrets:** fetched OPENAI/MASTER/`GHOST_DEMO_API_KEY` from Secret Manager and exported before the script (preserved existing GHOST_MASTER_KEY 44ch). Script re-upserted OPENAI v16, MASTER v16, GHOST_DEMO_API_KEY v2, and **regenerated GHOST_ADMIN_TOKEN → version 2** (the deploy script auto-generates a fresh admin token each run; operators using the previous `X-Ghost-Admin-Token` must pull the new value from Secret Manager).
- **Failures hit & fixes:** `firebase deploy --only hosting` failed TWICE with `Error: Task <hash> failed: retries exhausted after 6 attempts, with error: TypeError: Converting circular structure to JSON (TLSSocket)` — a transient firebase-tools upload bug on a large chunk (the ~984kB `pdf-*.js`), triggered by Node 24's network-family autoselection (Happy Eyeballs). **Fix that worked:** `export NODE_OPTIONS="--no-network-family-autoselection"` then re-run `firebase deploy --only hosting` → upload completed, release published. Backend had already deployed cleanly (revision `ghst-api-00014-rgs`) before the hosting step, so only hosting needed the retry. **Add this to the runbook:** if hosting upload dies on the TLSSocket circular-JSON error, retry with `NODE_OPTIONS=--no-network-family-autoselection`.
- **Live verify:** root 200, app.html 200, `/api/health` → `{"ok":true,"data":{"status":"healthy"}}`. `/api/users/demo/trial` returned 422 only because I posted an empty body (schema validation, not 503 — demo key is loaded). New Cloud Run revision `ghst-api-00014-rgs`, 100% traffic, URL `https://ghst-api-856742618724.us-central1.run.app`.
- **Auth state:** gcloud + firebase both valid as omer@ghost-il.com (no re-auth needed this run).
- **Version deployed:** v0.2.4.8.20 — cloud login marker screenshot confirmed `GHOST // BUILD — V0.2.4.8.20` (matches local), sent to Omer.
- **Still open (operator/manual):** (1) **seed self-knowledge in the cloud** — `ghost_self_knowledge` Chroma collection is NOT auto-populated; must run `cd backend && python scripts/seed_self_knowledge.py` against the cloud env (same `GHOST_MASTER_KEY` + GCS-mounted chroma path) so Expert mode + bounded "how do I use Ghost" doc retrieval/download offers work. Until then chat degrades gracefully to the 9-capability block (no errors). (2) Pre-existing security gaps still open: rotate+revoke the old exposed OpenAI demo key in the dashboard, and scrub it from git history (both operator-only).
