# 🗺️ הארכיטקטורה של Ghost — המדריך הוויזואלי

> מסמך זה מסביר **איך המערכת בנויה ומה קורה בכל רגע**, בשפה פשוטה ולא-טכנית.
> כל אחד — גם מי שלא מתכנת — אמור להבין מכאן את כל הסיפור, מקצה לקצה.

---

## 🎯 מה זו המערכת? — במשפט אחד

> **Ghost הוא "צופה לילה" חכם שיושב לידך, מסתכל יחד איתך על מצלמות האבטחה, ומספר לך במילים אנושיות מה הוא רואה — ואפילו זוכר מה ראה אתמול.**

אתה שואל אותו שאלות בצ'אט (בדיוק כמו צ'אט רגיל), הוא מסתכל על תמונה חיה מהמצלמה, ועונה לך.
הוא גם **שומר הכול אצלך במחשב** — אף אחד בחוץ לא רואה את המידע שלך.

---

## 🧩 מתוך מה המערכת מורכבת? — תמונת על

חשוב על המערכת כמו על **משרד עם שלושה חדרים** + יועץ חיצוני:

```mermaid
flowchart TB
    subgraph U["👤 אתה — המפעיל"]
        direction TB
        UI["מסך הצ'אט בדפדפן"]
    end

    subgraph FRONT["🖥️ חדר קדמי — מה שאתה רואה ולוחץ עליו (Frontend)"]
        direction TB
        F1["מסך הצ'אט, רשימת שיחות,<br/>תצוגת מצלמה, התראות"]
    end

    subgraph BACK["⚙️ חדר אחורי — המוח שמנהל הכול (Backend)"]
        direction TB
        B1["מקבל בקשות, מארגן מידע,<br/>מדבר עם ה-AI, שומר תוצאות"]
    end

    subgraph STORE["🗄️ ארכיון — איפה הכול נשמר (במחשב שלך)"]
        direction TB
        S1["מסד נתונים: שיחות, הודעות,<br/>זיכרונות, התראות, מצלמות"]
        S2["ארכיון תמונות:<br/>הפריימים שצולמו"]
    end

    subgraph AI["🧠 היועץ החיצוני — ה-AI"]
        direction TB
        A1["מנתח טקסט ותמונות<br/>ומחזיר תשובות"]
    end

    U --> FRONT
    FRONT <-->|"שאלות ותשובות"| BACK
    BACK <-->|"שמור / קרא"| STORE
    BACK <-->|"מה אתה רואה כאן?"| AI
```

**הכלל הזהב:** המידע שלך **לא יוצא מהמחשב**. הדבר היחיד שיוצא החוצה הוא הטקסט/התמונה שנשלחים ליועץ החיצוני (ה-AI) כדי לקבל תשובה.

---

## 🏠 ארבעת החלקים — כל אחד בשפה פשוטה

### 1. 🖥️ החדר הקדמי — מה שאתה רואה (Frontend)

זה הדף בדפדפן. מה שיש בו:

| מה אתה רואה | מה זה עושה |
| --- | --- |
| **רשימת שיחות** מימין | כמו רשימת צ'אטים בוואטסאפ — כל שיחה נשמרת |
| **חלון הצ'אט** במרכז | פה אתה כותב ופה מופיעות התשובות, מילה-מילה בזמן אמת |
| **תצוגת מצלמה** | תמונה חיה מהמצלמה/מצלמות שחיברת לשיחה |
| **מצב התראה** | מתג שמפעיל "שמירה אוטומטית" — המערכת מסתכלת לבד ומתריעה |
| **פאנל זיכרון / ידע** | מראה מה Ghost "זוכר" ואיזה מסמכים העלית לו |

### 2. ⚙️ החדר האחורי — המוח (Backend)

זה החלק שאתה **לא** רואה, אבל הוא עושה את כל העבודה החכמה:

- מקבל את מה שכתבת
- אוסף את כל ההקשר הרלוונטי (מה דובר קודם, מה Ghost זוכר, מסמכים שהעלית, מה ראה במצלמות)
- בונה "תדריך" מסודר ושולח ל-AI
- מקבל את התשובה ומעביר לך אותה מילה-מילה
- שומר הכול בארכיון לפעם הבאה

### 3. 🗄️ הארכיון — איפה נשמר המידע (במחשב שלך)

- **מסד נתונים** — טבלאות מסודרות: משתמשים, שיחות, הודעות, זיכרונות, חוקי התראה, מצלמות, "ישויות" שראינו.
- **ארכיון וקטורי** — דרך חכמה לחפש "לפי משמעות" ולא לפי מילים מדויקות (כך Ghost מוצא זיכרון רלוונטי גם אם ניסחת אחרת).
- **תיקיית תמונות** — כל פריים שצולם מהמצלמה נשמר כקובץ תמונה.

### 4. 🧠 היועץ החיצוני — ה-AI

זה המומחה שמנתח. שולחים לו טקסט ו/או תמונה, והוא מחזיר תיאור או תשובה. יש לו שלושה "כובעים":

| כובע | מתי משתמשים בו |
| --- | --- |
| **המנתח הראשי** | לענות לך בצ'אט ולתאר מה רואים במצלמה |
| **העוזר המהיר והזול** | לחלץ זיכרונות, לזהות מי/מה בתמונה, לסרוק התראות |
| **המאנדקס** | להפוך טקסט ל"טביעת משמעות" כדי לאפשר חיפוש חכם |

---

## 🚶 המסע: מה קורה בכל פעולה — צעד אחר צעד

זה החלק שמסביר **"כל דבר שקורה בדרך"**. כל זרימה מוצגת כדיאגרמה.

### א. 🔑 כניסה למערכת

```mermaid
sequenceDiagram
    autonumber
    participant אתה
    participant מסך as 🖥️ מסך הכניסה
    participant מוח as ⚙️ המוח
    participant ארכיון as 🗄️ הארכיון

    אתה->>מסך: מזין כינוי + מפתח אישי
    מסך->>מוח: בקשת התחברות
    מוח->>ארכיון: מי זה? המפתח נכון?
    ארכיון-->>מוח: כן, זה המשתמש ✓
    מוח-->>מסך: אישור כניסה
    מסך-->>אתה: נכנסת! רואה רק את השיחות שלך
```

> 🔒 המפתח האישי שלך נשמר **מוצפן** — גם מי שיפתח את הארכיון לא יוכל לקרוא אותו.

---

### ב. 💬 שליחת הודעת טקסט רגילה

```mermaid
sequenceDiagram
    autonumber
    participant אתה
    participant מסך as 🖥️ מסך הצ'אט
    participant מוח as ⚙️ המוח
    participant ארכיון as 🗄️ הארכיון
    participant AI as 🧠 היועץ

    אתה->>מסך: כותב הודעה ולוחץ שליחה
    מסך->>מסך: ההודעה שלך מופיעה מיד
    מסך->>מוח: שולח את ההודעה
    מוח->>ארכיון: שומר את ההודעה
    Note over מוח,ארכיון: אוסף הקשר:<br/>שיחה אחרונה + זיכרונות +<br/>מסמכים רלוונטיים + מה ראינו במצלמות
    מוח->>AI: "הנה כל הרקע + השאלה. מה התשובה?"
    AI-->>מוח: תשובה זורמת מילה-מילה
    מוח-->>מסך: כל מילה עוברת אליך בזמן אמת
    מסך-->>אתה: רואה את התשובה נכתבת לנגד עיניך
    מוח->>ארכיון: שומר את התשובה המלאה
    Note over מוח,AI: 🧠 ברקע: העוזר המהיר מחלץ<br/>עובדות חדשות וזוכר אותן לפעם הבאה
```

**מה חשוב כאן:** לפני שהשאלה נשלחת ל-AI, המוח **אורז עבורו תדריך מלא** — לא רק את השאלה, אלא גם את כל ההקשר. ככה התשובות נשמעות "מבינות" ולא מנותקות.

---

### ג. 📷 שליחת הודעה עם מבט מהמצלמה

```mermaid
sequenceDiagram
    autonumber
    participant אתה
    participant מצלמה as 📷 המצלמה
    participant מסך as 🖥️ מסך הצ'אט
    participant מוח as ⚙️ המוח
    participant AI as 🧠 היועץ
    participant ארכיון as 🗄️ הארכיון

    אתה->>מסך: "מה קורה בכניסה?" + המצלמה דולקת
    מסך->>מצלמה: צלם תמונה עכשיו
    מצלמה-->>מסך: תמונה חיה
    מסך->>מוח: השאלה + התמונה
    מוח->>AI: "הסתכל על התמונה הזו ותאר מה אתה רואה"
    AI-->>מוח: "אדם בכניסה, לבוש כהה, נראה ממתין..."
    מוח-->>מסך: התיאור זורם אליך
    מוח->>ארכיון: שומר את התמונה + התיאור
    Note over מוח,AI: 🧠 ברקע: מי/מה הופיע בתמונה?<br/>נשמר כ"ישות" עם שעה מדויקת
```

> כך נולד **הזיכרון החזותי** — לא רק "ראיתי אדם", אלא "ראיתי את האדם הזה ב-22:14 במצלמת הכניסה".

---

### ד. 📷📷 כמה מצלמות בבת אחת

אם חיברת לשיחה כמה מצלמות, הודעה אחת שלך מפעילה סבב על כולן:

```mermaid
flowchart LR
    Q["💬 שאלה אחת שלך"] --> M["⚙️ המוח"]
    M --> C1["📷 מצלמה 1<br/>→ תיאור נפרד"]
    M --> C2["📷 מצלמה 2<br/>→ תיאור נפרד"]
    M --> C3["📷 מצלמה 3<br/>→ תיאור נפרד"]
    C1 --> R["🖥️ תשובה לכל מצלמה<br/>מופיעה בנפרד"]
    C2 --> R
    C3 --> R
```

מקבלים תשובה **נפרדת וברורה לכל מצלמה**, אחת אחרי השנייה.

---

### ה. 🚨 מצב התראה — שמירה אוטומטית

זה הקסם הגדול: אתה לא צריך לשבת ולהסתכל. אתה אומר ל-Ghost **על מה להתריע** ("אדם עם נשק", "שריפה", "מישהו מטפס על הגדר"), ומדליק את המתג.

```mermaid
sequenceDiagram
    autonumber
    participant מצלמה as 📷 מצלמה (רצה ברקע)
    participant מוח as ⚙️ המוח
    participant AI as 🧠 העוזר המהיר
    participant מסך as 🖥️ המסך שלך

    loop כל חצי שנייה בערך
        מצלמה->>מוח: פריים חדש
        מוח->>AI: "האם רואים פה משהו מהחוקים שהוגדרו?"
        AI-->>מוח: בדרך כלל: "לא, הכול שקט"
    end
    Note over AI: רק כשבטוח מאוד שיש התאמה ⬇️
    AI-->>מוח: "כן! זוהה אדם מטפס על הגדר"
    מוח->>מסך: 🚨 התראה מיידית קופצת על המסך
    מסך-->>מסך: מצלצל + מציג את התמונה + התיאור
```

**שתי הגנות מובנות שמונעות התראות שווא:**
- 🎯 התראה נוצרת **רק כשה-AI בטוח מאוד** — לא על כל ספק.
- 🔁 המערכת לא שולחת אותה בדיקה פעמיים בו-זמנית.

---

### ו. ⏰ "מתי ראית את X?" — Ghost יודע להגיד שעה

בעבר AI היה עונה "אין לי יכולת לעקוב אחר זמנים". כאן זה פתור:

```mermaid
flowchart TB
    A["כל הודעה וכל תצפית<br/>מקבלת חותמת שעה ⏱️ [22:14]"] --> B["⚙️ המוח מצרף את השעות<br/>לתדריך שנשלח ל-AI"]
    B --> C["🧠 ה-AI יודע בדיוק<br/>מתי כל דבר קרה"]
    C --> D["אתה שואל: 'מתי ראית את הרכב הלבן?'"]
    D --> E["✅ תשובה: 'ב-22:14, במצלמת החניה'"]
```

---

## 🧠 המנגנונים החכמים שמאחורי הקלעים

ארבעה "כוחות-על" שגורמים ל-Ghost להרגיש חכם באמת:

```mermaid
mindmap
  root((Ghost))
    זיכרון
      אחרי כל שיחה לומד עובדות חדשות
      שומר אותן ושולף בשיחה הבאה
    בסיס ידע
      מעלים מסמכים PDF/Word/טקסט
      Ghost עונה לפי התוכן שלהם
    זיכרון חזותי
      זוכר מי ומה ראה בכל מצלמה
      אותו אדם בכמה מצלמות = רשומה אחת
    מגן השפה
      חוסם תשובות סירוב גנריות
      תמיד עונה בקול של Ghost
```

| הכוח | בשפה פשוטה |
| --- | --- |
| 🧠 **זיכרון** | אחרי כל שיחה, עוזר חכם מחלץ עובדות חשובות ושומר אותן. בפעם הבאה Ghost כבר "מכיר אותך". |
| 📚 **בסיס ידע** | מעלים מסמכים (נהלים, דוחות), Ghost חותך אותם לקטעים וזוכר. אחר כך עונה לפי מה שכתוב בהם. |
| 👁️ **זיכרון חזותי** | כל אדם/רכב שראינו במצלמה נשמר כ"ישות" עם שעה. אותו אדם שמופיע בכמה מצלמות מתאחד לרשומה אחת — כך אפשר לשאול "כמה אנשים שונים ראיתי הלילה?" ולקבל מספר מדויק. |
| 🛡️ **מגן השפה** | שתי שכבות שמוודאות שלעולם לא תקבל תשובת סירוב משעממת בנוסח "מצטער, אני לא יכול". במקום זה — הודעה ידידותית בקול של Ghost. |

---

## 🗄️ איפה כל פיסת מידע נשמרת?

```mermaid
flowchart LR
    subgraph DISK["💾 הכול על המחשב שלך — שום דבר בענני זרים"]
        direction TB
        DB[("📋 מסד נתונים<br/>שיחות · הודעות · משתמשים<br/>זיכרונות · התראות · מצלמות · ישויות")]
        VEC[("🔎 ארכיון 'לפי משמעות'<br/>לחיפוש חכם בזיכרון ובמסמכים")]
        IMG[("🖼️ תיקיית תמונות<br/>כל פריים שצולם מהמצלמה")]
    end
```

---

## 🔒 אבטחה ופרטיות — בקצרה

```mermaid
flowchart TB
    P1["🔐 המפתח האישי שלך<br/>נשמר מוצפן"]
    P2["🏠 כל המידע נשאר<br/>במחשב המקומי"]
    P3["👤 כל משתמש רואה<br/>רק את השיחות שלו"]
    P4["🌐 רק טקסט/תמונה לשאילתה<br/>נשלחים החוצה ל-AI"]
    P1 --- P2 --- P3 --- P4
```

---

## 🧭 סיכום ויזואלי — כל הסיפור בתמונה אחת

```mermaid
flowchart TB
    subgraph YOU["👤 אתה"]
        Y["שואל · מסתכל · מקבל התראות"]
    end

    subgraph SCREEN["🖥️ המסך (Frontend)"]
        SC["צ'אט · מצלמות · התראות · זיכרון"]
    end

    subgraph BRAIN["⚙️ המוח (Backend)"]
        BR["אוסף הקשר · בונה תדריך ·<br/>מנהל זרימה · שומר תוצאות"]
    end

    subgraph MEM["🗄️ הארכיון המקומי"]
        ME["נתונים · זיכרון · תמונות"]
    end

    subgraph BRAIN2["🧠 היועץ (AI)"]
        AI2["מנתח טקסט ותמונות"]
    end

    Y -->|"כותב / מצלם"| SC
    SC -->|"בקשה"| BR
    BR -->|"קורא היסטוריה + זיכרון"| ME
    BR -->|"מבקש ניתוח"| AI2
    AI2 -->|"תשובה זורמת"| BR
    BR -->|"שומר"| ME
    BR -->|"תשובה מילה-מילה"| SC
    SC -->|"מציג / מתריע"| Y
```

---

> **בשורה התחתונה:** אתה מדבר עם Ghost בצ'אט פשוט. מאחורי הקלעים, המוח אוסף את כל מה שצריך לדעת, מתייעץ עם ה-AI, זוכר מה שחשוב, שומר הכול אצלך בבית — ובמצב התראה אפילו שומר עליך לבד מסביב לשעון.
